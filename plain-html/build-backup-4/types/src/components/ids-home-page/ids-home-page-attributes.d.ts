declare const HOME_PAGE_DEFAULTS: {
    animated: boolean;
    cardHeight: number;
    cardWidth: number;
    cols: number;
    gap: number;
    gapX: number;
    gapY: number;
};
declare const EVENTS: {
    resized: string;
};
export { HOME_PAGE_DEFAULTS, EVENTS };
